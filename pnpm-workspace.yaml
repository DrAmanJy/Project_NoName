# ============================================================================
# AI AGENT NOTICE:
# Do NOT modify this file to suppress errors. You must fix the actual
# underlying code error. Read AGENTS.md before making any architectural
# or configuration changes.
# ============================================================================
packages:
  - "apps/*"
  - "packages/*"
allowBuilds:
  argon2: true
  esbuild: true
  unrs-resolver: true

node-linker: hoisted

nodeLinker: hoisted

virtualStoreDir: E:/.pnpm
